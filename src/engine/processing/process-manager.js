export default class ProcessManager {
    constructor() {
        this._processList = [];
    }

    updateProcesses(deltaMs) {
        console.warn('`ProcessManager.updateProcesses` method not implemented');
    }
}
